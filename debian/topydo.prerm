#!/bin/sh
set -e

case "$1" in
  remove)
    update-alternatives --remove todo.txt /usr/bin/topydo || true
    update-alternatives --remove todo /usr/bin/topydo || true
    ;;
esac

#DEBHELPER#

exit 0

